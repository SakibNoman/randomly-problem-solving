#include<bits/stdc++.h>
using namespace std;
int main()
{
    int nm,rd,magn,dif,per;
    while(scanf("%d%d",&nm,&rd)!=EOF)
    {
        if(nm<=rd)
        {
            dif=rd-nm;
            if(dif<=14)
            {
                per=(dif*100)/14;
            }
            else
            {
                
            }
        }
        else
        {

        }
    }
    return 0;
}